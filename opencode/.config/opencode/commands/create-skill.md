---
description: Create a custom skill for this project
---

Create a custom skill in the project's `.opencode/skill/` directory.

First, invoke the skill tool to load the create-skill skill:

```
skill({ name: 'create-skill' })
```

Then follow the skill instructions.

<user-request>
$ARGUMENTS
</user-request>
