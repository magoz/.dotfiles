---
description: Initialize a new project from the magoz/init template
---

Initialize a new project from the magoz/init template.

First, invoke the skill tool to load the init skill:

```
skill({ name: 'init' })
```

Then follow the skill instructions.

<user-request>
$ARGUMENTS
</user-request>
