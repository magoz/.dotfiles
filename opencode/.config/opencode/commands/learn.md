---
description: Capture patterns from the session and audit AGENTS.md files against the codebase
---

Capture session learnings and audit AGENTS.md documentation.

First, invoke the skill tool to load the learn skill:

```
skill({ name: 'learn' })
```

Then follow the skill instructions to capture and audit.

<user-request>
$ARGUMENTS
</user-request>
