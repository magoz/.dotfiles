---
description: Work with Paper designs — edit styles, modify text, export artboards to PDF with selectable text.
---

Work with Paper design tool via MCP.

First, invoke the skill tool to load the paper skill:

```skill({ name: 'paper' })```

Then follow the skill instructions.

<user-request>
$ARGUMENTS
</user-request>
