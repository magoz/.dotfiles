# ---------------------------------------------
#               ORIGINAL CONFIG
# https://github.com/Mach-OS/Machfiles/blob/6373a1fd1e42ca2fd8babd95ef4acce9164c86c3/zsh/.config/zsh/zsh-functions
#
# ---------------------------------------------

# Source an optional config file without hiding its errors.
function zsh_add_file() {
    local file="$ZDOTDIR/$1"
    [[ -r "$file" ]] || return 0
    source "$file"
}

